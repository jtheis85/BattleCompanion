{
  "name": "application-name",
  "version": "0.0.1",
  "dependencies": {
    "babel-preset-react": "^6.3.13",
    "babelify": "^7.2.0",
    "react": "^0.14.3",
    "react-dom": "^0.14.3"
  },
  "devDependencies": {
    "browserify": "^12.0.1"
  },
  "scripts": {
    "build": "browserify -t [ babelify --presets [ react ] ] src/battlecompanion.jsx -o bundle.js",
    "debug": "browserify --debug -t [ babelify --presets [ react ] ] src/battlecompanion.jsx -o bundle.js"
  }
}
