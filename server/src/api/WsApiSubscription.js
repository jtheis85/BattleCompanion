'use strict';

const subscribe = {
    service: 'event',
    action: 'subscribe'
};

const unsubscribe = {
    service: 'event',
    action: 'unsubscribe'
};

export {
    subscribe,
    unsubscribe
};