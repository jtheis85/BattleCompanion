'use strict';

class Faction {
    constructor(id, abbreviation, name) {
        this.id           = id;
        this.abbreviation = abbreviation;
        this.name         = name;
    }
}

export default Faction;